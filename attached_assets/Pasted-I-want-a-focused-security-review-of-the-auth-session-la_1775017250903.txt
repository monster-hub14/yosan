I want a focused security review of the auth/session layer.

Please verify the file(s) that handle JWT signing, verification, cookies, and route protection — likely:

* `artifacts/budget-app/src/lib/auth/session.ts`
* `artifacts/budget-app/src/middleware.ts`
* any login/logout/auth helper files

Please confirm the following with references to the actual code paths:

1. JWT handling

* JWTs are signed with `JWT_SECRET` only
* no hardcoded secret or fallback default exists in production
* token expiry is explicitly set
* token verification rejects invalid/expired tokens correctly

2. Cookie security

* session cookie is `httpOnly`
* `secure` is enabled in production
* `sameSite` is set intentionally
* cookie scope/path is appropriate
* cookie name is consistent and not duplicated across environments unintentionally

3. Session contents

* JWT payload contains only the minimum required claims
* no sensitive data is stored in the token (password hashes, API keys, etc.)
* role/permission data in the token is safe and validated server-side where needed

4. Middleware / route protection

* protected routes actually require a valid session
* admin-only routes are enforced server-side, not just hidden in the UI
* setup/onboarding redirects cannot be abused to bypass auth
* API routes are protected consistently, not only page routes

5. Failure behavior

* invalid or missing `JWT_SECRET` fails loudly in production
* invalid cookies/tokens are cleared or rejected safely
* no debug behavior leaks token contents or auth errors to clients

6. CSRF / auth flow checks

* confirm whether cookie-based authenticated POST/PATCH/DELETE routes have CSRF protection or another intentional mitigation
* if not, please flag that explicitly so we can decide whether to add CSRF defenses

Please give me:

* any confirmed issues
* any medium-risk design concerns
* any files that still need cleanup
* a final pass/fail assessment for production readiness of the auth/session layer
